# Tihloh Prefab PHP

Reusable, modular PHP components built to work standalone or integrate with frameworks like Laravel.

## Packages

- `tihloh/prefab-permissions` — framework-independent permissions and authorization module.

More prefabs can be added under `packages/` as independent Composer packages.
